# IssuePilot 环境变量示例
# 复制为 .env，填入真实值，不要提交 .env 到 git（已被 .gitignore 忽略）

# GitLab Access Token
# 来源：GitLab 项目 Settings → Access Tokens（角色 Maintainer，scope: api + read_repository + write_repository）
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# 如果使用 self-hosted GitLab，可在 workflow.md 的 tracker.base_url 里配置 URL，无需在此设置
